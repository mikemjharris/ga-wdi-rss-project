<div class='followers-users'>
	<div class='follow-title'>Followers</div>
<% @user.followers.each do |follower| %>
	<div class='article'><%= link_to "#{follower.first_name} #{follower.last_name}", timeline_user_path(follower), remote: true %></div>
<% end %>
</div>
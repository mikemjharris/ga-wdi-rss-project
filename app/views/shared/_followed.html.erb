<div class='followed-users'>
	<h2>Followed</h2>
<% @user.followed_users.each do |followed| %>
	<div class='article'><%= link_to "#{followed.first_name} #{followed.last_name}", timeline_user_path(followed), remote: true %></div>
<% end %>
</div>